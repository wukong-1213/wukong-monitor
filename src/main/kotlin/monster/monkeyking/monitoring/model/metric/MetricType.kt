package monster.monkeyking.monitoring.model.metric

enum class MetricType {
    CPU_USAGE,
    CPU_CORES,
    MEMORY_USED,
    MEMORY_TOTAL,
    DISK_USED,
    DISK_TOTAL,
    DISK_READ,
    DISK_WRITE,
    NETWORK_IN,
    NETWORK_OUT,
    PUBLIC_IP
}
